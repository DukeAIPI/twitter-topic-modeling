version: 1.0
runtime: python3 
build:
  commands:
    build:
      - sudo apt-get install gcc
      - pip install -r requirements.txt
run: 
  network: 
    port: 8501
  command: streamlit run streamlit.py
